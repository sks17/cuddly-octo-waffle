---
/**
 * Quantitative Trading Visual Component
 * 
 * Custom visual for the Quantitative Pairs Trading project card.
 * Features statistical analysis visualization with tech stack.
 */
---

<div class="quant-trading-visual">
  <div class="qt-container">
    <!-- Left: Title and Description -->
    <div class="qt-text-col">
      <div class="qt-header">
        <h2 class="qt-title">Quantitative Pairs Trading Research System</h2>
      </div>
      <div class="qt-description">
        <p>A quantitative research pipeline using pairs trading strategies. Downloads historical stock price data, identifies correlated pairs, generates mean-reversion signals using the Ornstein-Uhlenbeck process, and produces visualizations to validate trading assumptions and evaluate strategy performance.</p>
      </div>
    </div>

    <!-- Center: Z-Score Distribution Image -->
    <div class="qt-image-col">
      <div class="qt-chart">
        <img src="/assets/zscore_distribution.png" alt="Z-Score Distribution Analysis" class="qt-img" />
      </div>
    </div>

    <!-- Right: Technologies Table -->
    <div class="qt-tech-col">
      <div class="qt-tech">
        <h3 class="tech-title">Technologies</h3>
        <div class="tech-grid">
          <div class="tech-item">Python 3.10+</div>
          <div class="tech-item">Pandas</div>
          <div class="tech-item">NumPy</div>
          <div class="tech-item">SciPy</div>
          <div class="tech-item">Matplotlib</div>
          <div class="tech-item">scikit-learn</div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .quant-trading-visual {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #e8f5f0;
    position: relative;
    overflow: hidden;
  }

  /* Main container - 3 columns */
  .qt-container {
    display: grid;
    grid-template-columns: 1fr 400px 200px;
    gap: 2.5rem;
    align-items: center;
    max-width: 85%;
    width: 100%;
    padding: 0.5rem 2rem;
  }

  /* Left column: Title and Description */
  .qt-text-col {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .qt-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .qt-title {
    font-size: 2.2rem;
    font-weight: 700;
    color: var(--gray-0);
    margin: 0;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .qt-description {
    font-size: 1.05rem;
    color: var(--gray-200);
    line-height: 1.65;
    margin: 0;
  }

  .qt-description p {
    margin: 0;
  }

  /* Center column: Image */
  .qt-image-col {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .qt-chart {
    background: white;
    border: 2px solid rgba(147, 188, 207, 0.3);
    border-radius: 12px;
    padding: 1rem;
    width: 100%;
    box-shadow: 0 4px 12px rgba(147, 188, 207, 0.2);
  }

  .qt-img {
    width: 100%;
    height: auto;
    max-height: 350px;
    object-fit: contain;
    border-radius: 8px;
    display: block;
  }

  /* Right column: Technologies */
  .qt-tech-col {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .qt-tech {
    width: 100%;
  }

  .tech-title {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--gray-300);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
    border-bottom: 2px solid rgba(147, 188, 207, 0.4);
    padding-bottom: 0.4rem;
  }

  .tech-grid {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .tech-item {
    background: rgba(147, 188, 207, 0.15);
    border: 1px solid rgba(147, 188, 207, 0.3);
    border-radius: 6px;
    padding: 0.5rem 0.75rem;
    font-size: 0.68rem;
    font-weight: 500;
    color: var(--gray-100);
    text-align: center;
    transition: all 0.2s ease;
  }

  .tech-item:hover {
    background: rgba(147, 188, 207, 0.25);
    border-color: rgba(147, 188, 207, 0.5);
    transform: translateX(3px);
  }

  /* Responsive breakpoints */
  @media (max-width: 1400px) {
    .qt-container {
      grid-template-columns: 1fr 350px 180px;
      max-width: 90%;
    }

    .qt-title {
      font-size: 2rem;
    }

    .qt-description {
      font-size: 1rem;
    }

    .qt-chart {
      padding: 0.75rem;
    }

    .qt-img {
      max-height: 300px;
    }
  }

  @media (max-width: 1200px) {
    .qt-container {
      grid-template-columns: 1fr 300px 160px;
      gap: 2rem;
    }

    .qt-title {
      font-size: 1.8rem;
    }

    .qt-description {
      font-size: 0.95rem;
    }

    .qt-img {
      max-height: 250px;
    }
  }

  @media (max-width: 49.9em) {
    .qt-container {
      grid-template-columns: 1fr;
      grid-template-rows: auto auto auto;
      gap: 1.5rem;
      max-width: 95%;
      padding: 1rem;
    }

    .qt-text-col {
      order: 1;
    }

    .qt-image-col {
      order: 2;
    }

    .qt-tech-col {
      order: 3;
    }

    .qt-title {
      font-size: 1.6rem;
    }

    .qt-description {
      font-size: 0.9rem;
    }

    .qt-chart {
      padding: 0.5rem;
    }

    .qt-img {
      max-height: 200px;
    }

    .tech-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 0.5rem;
    }
  }
</style>
