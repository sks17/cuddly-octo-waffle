{
  "name": "far-flare-mock-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-dev mock of the external services the site consumes: Atlas (documents), Wallpaper/theme cache, Chat (SSE), and the GraphView API (graph nav, breadcrumbs, RAG context, search).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "snapshot": "tsx src/snapshot.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
