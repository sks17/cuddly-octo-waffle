---
---

<section class="showcase-wrapper">
	<div class="showcase-container">
		<span class="showcase-tag" aria-hidden="true">&gt; SinghDevs</span>
		<div class="showcase-visual">
			<img src="/assets/PaperPigeonDemo.gif" alt="Paper Pigeon 3D research visualization" class="showcase-gif" />
		</div>

		<div class="showcase-content">
			<div class="showcase-header">
				<img src="/assets/favicon-paperpigeon.jpeg" alt="Paper Pigeon Logo" class="showcase-logo" />
				<h2>Paper Pigeon</h2>
				<p class="showcase-tagline">Interactive 3D research discovery platform for the University of Washington</p>
			</div>

			<p class="showcase-description">
				Paper Pigeon transforms academic research discovery into an interactive experience. Visualize researcher networks, find collaborators through semantic resume matching, explore papers with AI-powered chat, and step into immersive VR environments.
			</p>

			<div class="showcase-features">
				<div class="feature-group">
					<h3>Core Capabilities</h3>
					<ul>
						<li>3D force-directed graph visualization of research networks</li>
						<li>Resume semantic matching with AWS Bedrock AI</li>
						<li>RAG-powered research chat with paper citations</li>
						<li>Immersive VR exploration with A-Frame</li>
						<li>Researcher profiles, publications, and affiliations</li>
					</ul>
				</div>

				<div class="feature-group">
					<h3>Technical Architecture</h3>
					<div class="tech-table">
						<div class="tech-row">
							<span class="tech-label">Frontend:</span>
							<span class="tech-value">React 19, TypeScript, Vite</span>
						</div>
						<div class="tech-row">
							<span class="tech-label">3D Engine:</span>
							<span class="tech-value">Three.js, 3d-force-graph, WebGL</span>
						</div>
						<div class="tech-row">
							<span class="tech-label">Backend:</span>
							<span class="tech-value">Flask, Vercel Serverless</span>
						</div>
						<div class="tech-row">
							<span class="tech-label">AI/Data:</span>
							<span class="tech-value">AWS Bedrock, DynamoDB, S3</span>
						</div>
						<div class="tech-row">
							<span class="tech-label">Infrastructure:</span>
							<span class="tech-value">Vercel CI/CD, Cloudflare Workers</span>
						</div>
					</div>
				</div>
			</div>

			<a href="/work/paper-pigeon" class="showcase-cta">View Full Project</a>
		</div>
	</div>
</section>

<style>
	.showcase-wrapper {
		width: 100%;
		background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(240, 240, 240, 0.02) 100%);
		border-top: 1px solid rgba(255, 255, 255, 0.1);
		border-bottom: 1px solid rgba(255, 255, 255, 0.1);
		padding: 4rem 2rem;
	}

	.showcase-container {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		position: relative;
	}

	.showcase-tag {
		position: absolute;
		top: 0.75rem;
		left: 0.75rem;
		display: flex;
		gap: 0.5rem;
		padding: 0.5rem 1rem;
		color: var(--accent-text-over);
		border: 1px solid var(--accent-regular);
		background-color: var(--accent-regular);
		border-radius: 0;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
		z-index: 4;
		pointer-events: none;
	}

	.showcase-visual {
		position: relative;
		aspect-ratio: 4 / 3;
		border-radius: 0.5rem;
		overflow: hidden;
		border: 1px solid rgba(255, 255, 255, 0.1);
		background: rgba(0, 0, 0, 0.2);
	}

	.showcase-gif {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.showcase-content {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.showcase-header {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.showcase-logo {
		width: 64px;
		height: 64px;
		border-radius: 0.5rem;
	}

	.showcase-header h2 {
		font-family: var(--font-brand);
		font-size: var(--text-4xl);
		font-weight: 600;
		color: var(--gray-0);
		margin: 0 0 0.5rem;
		letter-spacing: -0.025em;
	}

	.showcase-tagline {
		font-size: var(--text-md);
		color: var(--gray-400);
		margin: 0;
		font-weight: 500;
	}

	.showcase-description {
		font-size: var(--text-lg);
		line-height: 1.6;
		color: var(--gray-200);
		margin: 0;
	}

	.showcase-features {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.feature-group {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.feature-group h3 {
		font-size: var(--text-lg);
		font-weight: 600;
		color: var(--gray-100);
		margin: 0;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.875rem;
	}

	.feature-group ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.feature-group li {
		font-size: var(--text-sm);
		color: var(--gray-300);
		padding-left: 1.5rem;
		position: relative;
		line-height: 1.5;
	}

	.feature-group li::before {
		content: '▸';
		position: absolute;
		left: 0;
		color: var(--accent-regular);
	}

	.tech-table {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		background: rgba(0, 0, 0, 0.2);
		padding: 1.5rem;
		border-radius: 0.375rem;
		border: 1px solid rgba(255, 255, 255, 0.05);
	}

	.tech-row {
		display: grid;
		grid-template-columns: 120px 1fr;
		gap: 1rem;
		font-size: var(--text-sm);
		align-items: baseline;
	}

	.tech-label {
		color: var(--gray-400);
		font-weight: 500;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.75rem;
	}

	.tech-value {
		color: var(--gray-200);
		font-family: 'Public Sans', sans-serif;
	}

	.showcase-cta {
		align-self: flex-start;
		padding: 0.875rem 2rem;
		background: var(--accent-regular);
		color: var(--gray-999);
		border-radius: 0.375rem;
		text-decoration: none;
		font-weight: 600;
		font-size: var(--text-sm);
		transition: all 0.3s ease;
		cursor: pointer;
		border: 1px solid var(--accent-regular);
	}

	.showcase-cta:hover {
		background: var(--accent-dark);
		border-color: var(--accent-dark);
		transform: translateY(-2px);
	}

	@media (max-width: 768px) {
		.showcase-container {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.showcase-header h2 {
			font-size: var(--text-3xl);
		}

		.tech-row {
			grid-template-columns: 100px 1fr;
		}

		.showcase-wrapper {
			padding: 2rem 1.5rem;
		}
	}
</style>

<style>
	.showcase-wrapper {
		width: 100%;
		background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(240, 240, 240, 0.02) 100%);
		border-top: 1px solid rgba(255, 255, 255, 0.1);
		border-bottom: 1px solid rgba(255, 255, 255, 0.1);
		padding: 4rem 2rem;
	}

	.showcase-container {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
	}

	.showcase-visual {
		position: relative;
		aspect-ratio: 4 / 3;
		border-radius: 0.5rem;
		overflow: hidden;
		border: 1px solid rgba(255, 255, 255, 0.1);
		background: rgba(0, 0, 0, 0.2);
	}

	.showcase-gif {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.showcase-content {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.showcase-header h2 {
		font-family: var(--font-brand);
		font-size: var(--text-4xl);
		font-weight: 600;
		color: var(--gray-0);
		margin: 0 0 0.5rem;
		letter-spacing: -0.025em;
	}

	.showcase-tagline {
		font-size: var(--text-md);
		color: var(--gray-400);
		margin: 0;
		font-weight: 500;
	}

	.showcase-description {
		font-size: var(--text-lg);
		line-height: 1.6;
		color: var(--gray-200);
		margin: 0;
	}

	.showcase-features {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.feature-group {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.feature-group h3 {
		font-size: var(--text-lg);
		font-weight: 600;
		color: var(--gray-100);
		margin: 0;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.875rem;
	}

	.feature-group ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.feature-group li {
		font-size: var(--text-sm);
		color: var(--gray-300);
		padding-left: 1.5rem;
		position: relative;
		line-height: 1.5;
	}

	.feature-group li::before {
		content: '▸';
		position: absolute;
		left: 0;
		color: var(--accent-regular);
	}

	.tech-table {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		background: rgba(0, 0, 0, 0.2);
		padding: 1.5rem;
		border-radius: 0.375rem;
		border: 1px solid rgba(255, 255, 255, 0.05);
	}

	.tech-row {
		display: grid;
		grid-template-columns: 120px 1fr;
		gap: 1rem;
		font-size: var(--text-sm);
		align-items: baseline;
	}

	.tech-label {
		color: var(--gray-400);
		font-weight: 500;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.75rem;
	}

	.tech-value {
		color: var(--gray-200);
		font-family: 'Public Sans', sans-serif;
	}

	.showcase-cta {
		align-self: flex-start;
		padding: 0.875rem 2rem;
		background: var(--accent-regular);
		color: var(--gray-999);
		border-radius: 0.375rem;
		text-decoration: none;
		font-weight: 600;
		font-size: var(--text-sm);
		transition: all 0.3s ease;
		cursor: pointer;
		border: 1px solid var(--accent-regular);
	}

	.showcase-cta:hover {
		background: var(--accent-dark);
		border-color: var(--accent-dark);
		transform: translateY(-2px);
	}

	@media (max-width: 768px) {
		.showcase-container {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.showcase-header h2 {
			font-size: var(--text-3xl);
		}

		.tech-row {
			grid-template-columns: 100px 1fr;
		}

		.showcase-wrapper {
			padding: 2rem 1.5rem;
		}
	}
</style>
