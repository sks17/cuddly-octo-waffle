---
/**
 * WALLS Visual Component
 * 
 * Custom visual for the WALLS Mental Health Assessment Platform project card.
 * Features three-column layout with logo, description, and tech stack.
 */
---

<div class="walls-visual">
  <div class="walls-container">
    <!-- Left: Demo GIF -->
    <div class="walls-logo-col">
      <div class="walls-header">
        <img src="/assets/WallsDemo2.gif" alt="WALLS Platform Demo" class="walls-logo" />
      </div>
    </div>

    <!-- Center: Description -->
    <div class="walls-text-col">
      <div class="walls-description">
        <h2 class="walls-title">WALLS Mental Health Assessment Platform</h2>
        <p>A comprehensive mental health assessment platform using PyTorch for stress, anxiety, and depression screening. Features Flask backend with dual-mode storage (Firebase/CSV), interactive Chart.js dashboards, Three.js visualizations, and local demo mode for offline presentations.</p>
      </div>
    </div>

    <!-- Right: Technologies -->
    <div class="walls-tech-col">
      <div class="walls-tech">
        <h3 class="tech-title">Technologies</h3>
        <div class="tech-grid">
          <div class="tech-item">Python 3.12</div>
          <div class="tech-item">Flask</div>
          <div class="tech-item">PyTorch</div>
          <div class="tech-item">Chart.js</div>
          <div class="tech-item">Three.js</div>
          <div class="tech-item">Firebase</div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .walls-visual {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #5c9eac;
    position: relative;
    overflow: hidden;
  }

  /* Main container - 3 columns */
  .walls-container {
    display: grid;
    grid-template-columns: 520px minmax(auto, 650px) 200px;
    gap: 2.5rem;
    align-items: center;
    max-width: 85%;
    width: 100%;
    padding: 0.5rem 2rem;
  }

  /* Left column: Logo */
  .walls-logo-col {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .walls-header {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .walls-logo {
    width: 100%;
    max-width: 494px;
    height: auto;
    display: block;
    border-radius: 12px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
  }

  /* Center column: Text */
  .walls-text-col {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .walls-title {
    font-size: 2.2rem;
    font-weight: 700;
    color: white;
    margin: 0 0 0.75rem 0;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .walls-description {
    color: rgba(255, 255, 255, 0.95);
  }

  .walls-description p {
    font-size: 1.05rem;
    line-height: 1.65;
    margin: 0;
  }

  /* Right column: Technologies */
  .walls-tech-col {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .walls-tech {
    width: 100%;
  }

  .tech-title {
    font-size: 0.85rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
    border-bottom: 2px solid rgba(255, 255, 255, 0.3);
    padding-bottom: 0.4rem;
  }

  .tech-grid {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .tech-item {
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 6px;
    padding: 0.5rem 0.75rem;
    font-size: 0.68rem;
    font-weight: 500;
    color: white;
    text-align: center;
    transition: all 0.2s ease;
  }

  .tech-item:hover {
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateX(3px);
  }

  /* Responsive breakpoints */
  @media (max-width: 1400px) {
    .walls-container {
      grid-template-columns: 450px minmax(auto, 600px) 180px;
      max-width: 90%;
    }

    .walls-title {
      font-size: 2rem;
    }

    .walls-description p {
      font-size: 1rem;
    }

    .walls-logo {
      max-width: 430px;
    }
  }

  @media (max-width: 1200px) {
    .walls-container {
      grid-template-columns: 380px minmax(auto, 500px) 160px;
      gap: 2rem;
    }

    .walls-title {
      font-size: 1.8rem;
    }

    .walls-description p {
      font-size: 0.95rem;
    }

    .walls-logo {
      max-width: 360px;
    }
  }

  @media (max-width: 1000px) {
    .walls-container {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      max-width: 95%;
      padding: 2rem 1.5rem;
    }

    .walls-logo-col {
      order: 1;
    }

    .walls-text-col {
      order: 2;
      text-align: center;
    }

    .walls-tech-col {
      order: 3;
      max-width: 300px;
      margin: 0 auto;
    }

    .walls-title {
      font-size: 2rem;
    }

    .walls-logo {
      max-width: 400px;
    }

    .tech-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 0.5rem;
    }
  }

  @media (max-width: 600px) {
    .walls-container {
      padding: 1.5rem 1rem;
    }

    .walls-title {
      font-size: 1.6rem;
    }

    .walls-description p {
      font-size: 0.9rem;
    }

    .walls-logo {
      max-width: 280px;
    }
  }
</style>
